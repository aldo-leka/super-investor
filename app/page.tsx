import { Button, Typography, Container, Grid, Card, CardContent } from '@mui/material';

export default function Home() {
  return (
    <Container maxWidth="lg">
      <Typography variant="h6" component="h1">
        Welcome to Super Investor
      </Typography>
    </Container>
  );
}